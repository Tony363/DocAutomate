# Spreadsheet Submission Example (Placeholder)

Steps (to be expanded with real data):

1. Upload `.xlsx` file using `POST /documents/upload`.
2. Verify `delegation_status` is `preprocessed` and `document_type` is `spreadsheet`.
3. Review extracted actions and ensure quality score ≥ 0.95; if not, inspect manual review queue.

Benchmark output and anonymized samples will be attached after workflow tuning completes.
